baseURL = "https://btrplcmnt.sim.ilitu.de/"
title = "Btrplcmnt"
defaultContentLanguage = 'de'
defaultContentLanguageInSubdir = true
[languages]
  [languages.de]
    # contentDir = 'content/de'
    disabled = false
    languageCode = 'de-DE'
    languageDirection = 'ltr'
    languageName = 'Deutsch'
    weight = 1
    [languages.de.params]
      flag = ':de:'
      subtitle = 'Better* Designsystem gegründet auf Felsen'
      titleCta = 'Guck mal, diese Farben!'
  [languages.en]
    # contentDir = 'content/en'
    disabled = false
    languageCode = 'en-US'
    languageDirection = 'ltr'
    languageName = 'English'
    weight = 2
    [languages.en.params]
      flag = ':gb:'
      subtitle = 'Better* design system based on rocks'
      titleCta = 'Look at these colors!'

# theme = "btrplcmnt"
[markup.goldmark.renderer]
  unsafe = true
[markup.highlight]
	noClasses = false
[params]
  cssPropPrefix = '🧈'
  colorSteps = [0, 100, 200, 300, 400, 500, 600, 700, 800, 900, 1000]
  colorPalettes = ['grey', 'primary', 'secondary', 'tertiary', 'quarternary', 'quinary']
[module]
[[module.mounts]]
  source = 'static'
  target = 'static'
[[module.mounts]]
  source = 'node_modules/@fontsource/fira-sans/files'
  target = 'static/fonts/files'
  includeFiles = '*.woff?'
